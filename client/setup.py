from setuptools import setup, find_packages

setup(
    name='s1_client_test',
    version='0.3.0',
    packages=find_packages(),
    install_requires=[],
    entry_points={
        'console_scripts': [],
    },
    author='cialabs'
)
